import 'package:flutter/material.dart';

import 'components/login_body.dart';

class LoginScreen extends StatelessWidget {
  static const routeName = "/login_screem";
  @override
  Widget build(BuildContext context) {
    return LoginBody();
  }
}
